#include <stdio.h>
#include "function.h"
int main(void)
{
    int choice;
    printf("***Menu chuong trinh***\n");
    printf("1.In so le tu 1 den n \n");
    scanf("%d", &choice);
    switch (choice)
    {
    case 1:
        printf("Ban da chon in so tu 1 den %d", n);
        insole(int n)
        break;
    }
}
return 0;